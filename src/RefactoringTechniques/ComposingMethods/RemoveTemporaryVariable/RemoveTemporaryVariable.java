package RefactoringTechniques.ComposingMethods.RemoveTemporaryVariable;

public class RemoveTemporaryVariable {
    // Sometimes temps can get in the way.
    // To get rid of a temp:
    // 1. Declare it as final to make sure it is declared only once
    // 2. Replace the right side with the temp name
}

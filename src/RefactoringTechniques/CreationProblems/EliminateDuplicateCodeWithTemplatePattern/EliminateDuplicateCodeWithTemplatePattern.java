package RefactoringTechniques.CreationProblems.EliminateDuplicateCodeWithTemplatePattern;

public class EliminateDuplicateCodeWithTemplatePattern {

    public static void main(String[] args) {
        // - template method = when a generalization is used to transform specific code into general purpose code
        // - use template method to simplify or eliminate duplicate code
        // - use when objects perform similar steps in the same order
        //      - implement the similar code in a template method
        //      - allow subclasses to override the behaviour that varies
    }
}
